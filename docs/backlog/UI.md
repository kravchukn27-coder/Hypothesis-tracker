# UI Backlog

## UI-045 — Привести кнопки карточки эксперимента к стилю Backlog

- **Status:** TODO
- **Priority:** Medium
- **Area:** UI / Experiments
- **Type:** UI fix
- **Summary:** На `/experiments/[id]` «Показать на календаре» сейчас просто подчёркнутая ссылка (не похожа на кнопку), а «Гипотеза» — крупный блок на всю ширину формы. Пользователь прислал скриншот с разметкой: сделать «Показать на календаре» настоящей кнопкой в стиле Backlog, а «Гипотезу» — небольшой кнопкой и перенести её наверх.
- **Description:**
  Скриншот пользователя показывает `/experiments/[id]`: справа в hero-блоке обведена ссылка «Показать на календаре», снизу в форме обведено крупное поле «Гипотеза» с именем гипотезы и стрелкой.

  Эталон — уже существующие кнопки на `/backlog/[id]` (`src/app/backlog/[id]/page.tsx`):
  - Кнопка-ссылка типа `rounded-lg border border-zinc-300 px-4 py-2.5 text-sm font-medium text-zinc-700 transition-colors hover:bg-zinc-50` — так там выглядят «Карточка эксперимента» (в шапке, ~строка 83) и «Открыть в Calendar» (в hero-блоке, ~строка 145).
  - «Показать на календаре» на `/experiments/[id]` (`src/app/experiments/[id]/page.tsx`, ~строка 129-136) сейчас — `<Link>` с классами `inline-flex items-center gap-2 font-medium text-zinc-900 underline underline-offset-4` (просто подчёркнутый текст с иконкой) — нужно заменить на тот же класс кнопки, что у «Открыть в Calendar» в Backlog.
  - «Гипотеза» на `/experiments/[id]` сейчас живёт не в самой странице, а внутри `ExperimentForm.tsx` (~строка 124-135) — это `<Link>` на всю ширину формы (`w-full`, `rounded-lg border`, `py-2.5`) с именем гипотезы `text-base font-semibold` и иконкой `ArrowRight`. Нужно превратить в компактную кнопку (по аналогии с «Карточка эксперимента» в Backlog — та же кнопка-класс, короткая подпись типа «Гипотеза» или название гипотезы без крупного шрифта) и перенести из тела формы наверх страницы — в `experiments/[id]/page.tsx`, рядом с «Архивировать»/«Удалить» в шапке (как у Backlog «Карточка эксперимента») или в сам hero-блок.
- **Acceptance Criteria:**
  - «Показать на календаре» на `/experiments/[id]` визуально — кнопка с рамкой (`rounded-lg border border-zinc-300 ...`), а не просто подчёркнутый текст, тем же классом, что «Открыть в Calendar» на `/backlog/[id]`.
  - Переход на гипотезу представлен небольшой кнопкой (не крупным блоком на всю ширину формы), расположенной в верхней части страницы `/experiments/[id]` — рядом с «Архивировать»/«Удалить» или в hero-блоке, а не внутри `ExperimentForm`.
  - Ссылка по-прежнему ведёт на `/backlog/${hypothesis.id}` — само поведение перехода не меняется.
  - Скрытое поле `hypothesisId` (сабмитится вместе с формой) сохраняется рабочим независимо от того, где физически рендерится кнопка.
  - Funnel Level гипотезы редактируется тем же способом, что и сейчас (`FunnelLevelField` в `ExperimentForm`) — эта задача не трогает саму форму редактирования, только расположение и стиль кнопки-ссылки на гипотезу.
  - Не меняются Backlog, Calendar и доменная логика архивирования/удаления/связи гипотеза↔эксперимент.

## UI-044 — Увеличить заголовок «Hypothesis Tracker» и убрать «Пользователи»/«События»/«Выйти» в бургер-меню

- **Status:** DONE
- **Priority:** Medium
- **Area:** UI / Layout
- **Type:** UI fix
- **Summary:** В хедере приложения (`src/app/layout.tsx`) два отдельных изменения: (1) увеличить размер надписи «Hypothesis Tracker», (2) спрятать пункты «Пользователи», «События» и кнопку «Выйти» в выпадающее мини-меню по иконке трёх горизонтальных полосок (гамбургер), не трогая Backlog/Calendar в основной навигации.
- **Description:**
  Скриншот пользователя, две отметки:
  1. Надпись «Hypothesis Tracker» слева в хедере (`src/app/layout.tsx:39-41`, `<span className="text-lg font-semibold tracking-tight text-zinc-900">`) сейчас `text-lg` — визуально мельче, чем хотелось бы как заголовок бренда приложения. Увеличить размер шрифта (например до `text-xl`/`text-2xl` — подобрать при реализации).
  2. Справа в хедере сейчас видны отдельными элементами: `NavLinks` (`src/app/NavLinks.tsx`) со всеми четырьмя пунктами — Backlog, Calendar, Пользователи, События — рендерит их одинаковыми pill-кнопками без разделения по важности; и рядом, в `layout.tsx:44-49`, форма логаута с именем пользователя и кнопкой «Выйти». Пользователь хочет оставить в основной навигации только Backlog/Calendar (это две главные рабочие вкладки), а «Пользователи», «События» и «Выйти» спрятать за выпадающее мини-меню, открывающееся по иконке трёх горизонтальных полосок (гамбургер) — как компактный вторичный/админский блок, а не наравне с основными разделами.
- **Acceptance Criteria:**
  - Надпись «Hypothesis Tracker» в хедере крупнее текущего `text-lg` (конкретный размер подобрать по вкусу при реализации, сохраняя `font-semibold tracking-tight`).
  - В хедере остаётся видимая основная навигация только на Backlog и Calendar (`NavLinks`, либо её сокращённая версия).
  - Ссылки «Пользователи» и «События», а также кнопка «Выйти» (текущая форма логаута с именем пользователя) перенесены в выпадающее меню, открывающееся по иконке-гамбургеру (три горизонтальные полоски) в правой части хедера.
  - Само меню открывается/закрывается по клику на иконку (и, желательно, по клику вне меню — паттерн уже используется в `RolloutCell.tsx`/аналогичных компонентах с `createPortal`+click-outside — переиспользовать при возможности).
  - Поведение самого логаута (`logout` action, `src/lib/auth/actions.ts`) и переходов на `/users`/`/activity` не меняется — меняется только их расположение/видимость в хедере.
  - Активное состояние (`aria-current`/подсветка) для Backlog/Calendar в основной навигации сохраняется как сейчас.

## UI-043 — Отделить кнопку «Показать все эксперименты» от навигации по неделям в Calendar

- **Status:** TODO
- **Priority:** Low
- **Area:** UI / Calendar
- **Type:** UI fix
- **Summary:** Кнопка «Показать все эксперименты» стоит в одном ряду и без визуального разделения с блоком навигации по неделям (Сегодня / ‹ / ›), из-за чего переключение режима (таймлайн ↔ полный список) путается с навигацией по датам. Перенести её левее, подальше от этой группы.
- **Description:**
  Скриншот пользователя: стрелка указывает на разрыв между заголовком «Calendar» и группой кнопок справа, где «Показать все эксперименты» стоит прямо перед «Сегодня»/‹/› без отступа или разделителя. Текущая вёрстка (`src/app/calendar/page.tsx:194-228`) кладёт обе группы в один общий `<div className="flex items-center gap-2">`: сначала `Link` с `href={showAll ? calendarHref() : "/calendar?calendarView=all"}` (переключатель режима, текст «Показать все эксперименты» / «Таймлайн»), затем (только когда `!showAll`) «Сегодня» и стрелки ‹/›. Визуально все четыре элемента выглядят одной однородной группой кнопок, хотя по смыслу это два разных действия: переключение режима отображения и навигация по датам внутри таймлайна.
- **Acceptance Criteria:**
  - Кнопка «Показать все эксперименты» (и её обратный вариант «Таймлайн» в режиме списка) визуально отделена от группы «Сегодня»/‹/› — например, перенесена к левому краю хедера рядом с заголовком/счётчиком «Calendar», либо оставлена справа, но в отдельной группе с явным отступом/разделителем от Сегодня/‹/›. Конкретный вариант выбрать при реализации.
  - Клик по кнопке и её текущее поведение (переключение `calendarView=all`, сохранение `experimentId`/фильтров через `calendarHref()`) не меняются.
  - Навигация «Сегодня»/‹/› и её текущая логика (disabled-состояние на «Сегодня», сохранение фильтров) не меняются.
  - Правка не переносится на другие элементы хедера (счётчик экспериментов, заголовок «Calendar»), если это не требуется выбранным вариантом.

## UI-042 — Три UI-фикса в карточке Backlog (кнопка Сохранить, «Открыть в Calendar», ширина Описания)

- **Status:** TODO
- **Priority:** Medium
- **Area:** UI / Backlog
- **Type:** UI fix
- **Summary:** На скриншоте карточки `/backlog/[id]` пользователь отметил три отдельные проблемы: (1) кнопка «Сохранить» висит слишком далеко от правого края рабочей области, (2) ссылка «Открыть в Calendar» выглядит как обычный текстовый линк, а не кнопка, (3) поле «Описание» в форме растянуто на всю ширину карточки и должно быть вдвое уже.
- **Description:**
  1. **Кнопка «Сохранить» далеко от края.** Форма использует общий `StickyFormSubmit` (`src/components/StickyFormSubmit.tsx`) — кнопка зафиксирована через `fixed inset-x-4 bottom-4 ... lg:right-8 lg:left-auto`, то есть привязана к правому краю **окна браузера**, а не к правому краю самой карточки/рабочей области контента (карточка у `HypothesisForm`/`ExperimentForm` заметно уже, чем viewport на широких экранах — отсюда зазор на скриншоте). Компонент общий для двух форм: `HypothesisForm.tsx` (`src/app/backlog/HypothesisForm.tsx:231`) и `ExperimentForm.tsx` (`src/app/experiments/ExperimentForm.tsx:234`) — правка в `StickyFormSubmit.tsx` автоматически чинит обе карточки (Backlog и Experiments), как и попросил пользователь.
  2. **«Открыть в Calendar» — не кнопка.** Сейчас это подчёркнутый текстовый линк (`src/app/backlog/[id]/page.tsx:143`): `<Link href={\`/calendar?experimentId=${activeExperiment.id}\`} className="font-medium text-zinc-900 underline underline-offset-4">Открыть в Calendar</Link>`, расположенный во втором ряду hero-блока карточки (рядом со статусом/Score/Funnel Level badges). Нужно привести его к виду обычной кнопки — визуально как соседние кнопки в хедере карточки («Карточка эксперимента», «Архивировать», «Удалить»: `rounded-lg border border-zinc-300 px-4 py-2.5 text-sm font-medium ...`, см. `src/app/backlog/[id]/page.tsx:80-90`), не меняя маршрут/поведение (переход на `/calendar?experimentId=...`).
  3. **Поле «Описание» вдвое уже.** `HypothesisForm.tsx:106-115` — `<Field label="Описание" htmlFor="text"><Textarea ... rows={6} /></Field>` сейчас без ограничения ширины, растягивается на всю ширину карточки (в отличие от блока ниже, который уже использует `grid gap-6 sm:grid-cols-2` для пары полей). Сделать поле вдвое уже — уточнить при реализации, как именно (например, обернуть в `sm:grid-cols-2` с пустой второй колонкой, либо задать `max-w-[50%]`/аналог) и нужно ли применить то же к аналогичному полю в `ExperimentForm.tsx`, если оно там есть (пользователь скриншотил только карточку Backlog/гипотезы).
- **Acceptance Criteria:**
  - Кнопка «Сохранить» визуально ближе к правому краю рабочей области/карточки (а не к краю всего окна браузера) в обеих формах — Backlog (`/backlog/[id]`, `/backlog/new`) и Experiments (`/experiments/[id]`, `/experiments/new`) — за счёт правки общего `StickyFormSubmit`.
  - Ссылка «Открыть в Calendar» на `/backlog/[id]` выглядит как кнопка (визуально согласована с соседними кнопками хедера карточки), сохраняя текущий href и условие показа (только когда есть `activeExperiment`).
  - Поле «Описание» в форме гипотезы (`HypothesisForm.tsx`) вдвое уже текущей ширины; решение по симметричному полю в `ExperimentForm.tsx` и по конкретной технике сужения принимается при реализации.
  - Не меняются доменная логика, Prisma-схема, поведение сохранения формы и переход в Calendar.

## UI-039 — Перенести значок фильтра в заголовке колонки Funnel Level направо

- **Status:** TODO
- **Priority:** Low
- **Area:** UI / Backlog
- **Type:** UI fix
- **Summary:** В шапке таблицы Backlog значок фильтра у колонки «Funnel Level» стоит слева от текста, а у колонки «Status» — справа. Перенести значок у Funnel Level направо, чтобы оба заголовка выглядели одинаково.
- **Description:**
  Скриншот пользователя: в шапке `Funnel Level` иконка воронки-фильтра стоит перед текстом («🔽 Funnel Level»), тогда как `Status` показывает текст с сортировкой, а иконка фильтра стоит уже после него, справа. Причина расхождения — в разной композиции: колонка `Status` (`src/app/backlog/page.tsx` ~строка 207) собрана из двух отдельных элементов, `SortableHeader` (текст + стрелка сортировки) и следом `HeaderMultiFilter` с пустым `label=""` (рендерит только сам значок) — из-за пустого label порядок иконки внутри `HeaderMultiFilter` для Status визуально не виден. Колонка `Funnel Level` (~строка 219) не сортируемая и использует только один `HeaderMultiFilter` с `label="Funnel Level"` — а внутри самого компонента (`src/components/HeaderMultiFilter.tsx`, JSX кнопки-триггера) иконка `<Filter />` рендерится ДО `{label}`, поэтому именно здесь порядок «иконка → текст» становится заметен.

  Фикс — в самом `HeaderMultiFilter.tsx`: поменять местами `<Filter />` и `{label}{selected.size ? ... : ""}` внутри кнопки-триггера, чтобы иконка шла после текста. Это общий переиспользуемый компонент (тот же, что и у Status), так что фикс автоматически выравнивает и другие места, использующие `HeaderMultiFilter` с непустым `label` — например Calendar-фильтр «Автор» (`src/app/calendar/page.tsx`, `HeaderMultiFilter name="calendarAuthor" label="Автор" ...`), у которого сейчас та же проблема, хоть пользователь и не упомянул её явно на скриншоте.
- **Acceptance Criteria:**
  - В заголовке колонки Funnel Level в Backlog значок фильтра стоит справа от текста «Funnel Level», как у Status.
  - Поведение самого фильтра (открытие панели, выбор нескольких значений, применение через URL-параметры) не меняется — правка чисто визуальная.
  - Calendar-фильтр «Автор» (тот же `HeaderMultiFilter`) получает то же выравнивание значка справа как побочный эффект переиспользования компонента — не ломается и не требует отдельной правки.
  - Не меняются `SortableHeader`, сортировка Status и остальные колонки таблицы.

## UI-023 — Сделать редактирование недельного плана в Calendar обнаружимым и безопасным

- **Status:** DONE
- **Priority:** Medium
- **Area:** UI / UX
- **Type:** Enhancement
- **Summary:** Сохранить прямое редактирование недельной сетки Calendar, но сделать перенос и изменение длительности понятными, подтверждаемыми и дополнить сетку вторичным сигналом нагрузки.
- **Description:**
  Calendar — основной desktop-инструмент для контроля текущих экспериментов, смены стадий, планирования следующих недель и оценки распределения нагрузки. Сохраняем существующие прямые жесты: клик по неделе для стадии, drag для переноса и изменение длительности за край блока; не добавляем постоянные отдельные кнопки действий. При наведении и keyboard-focus блок должен явно сообщать, что его можно переносить, а правый край — растягивать. При первом релевантном взаимодействии нужна короткая ненавязчивая подсказка. Перенос и изменение длительности сначала показывают preview нового диапазона и требуют подтверждения; смена стадии одной недели остаётся быстрым действием с явной обратной связью. В заголовке каждой недели добавить компактный вторичный показатель количества активных экспериментов, не конкурирующий с датой и номером недели.
- **Acceptance Criteria:**
  - Клик по недельной ячейке, перенос блока и изменение его длительности остаются доступными без постоянных отдельных кнопок.
  - При наведении и keyboard-focus видно, что блок можно переносить, а его край — растягивать; пользователь получает краткую подсказку о доступных действиях в первом релевантном сценарии.
  - Перед сохранением переноса или изменения длительности показывается новый недельный диапазон и требуется явное подтверждение; отмена не меняет план.
  - После смены стадии недели, подтверждения изменения плана и ошибки сохранения пользователь получает однозначную контекстную обратную связь.
  - Заголовок каждой недели показывает компактное вторичное количество активных экспериментов, не нарушая приоритет даты и номера недели.
  - Сохраняются недельная модель данных, расчёт текущей стадии, фильтры, правила скрытия Done, редактирование из карточки эксперимента и существующий desktop-first подход.
  - Не меняются Prisma-схема, доменная логика, карточки экспериментов и мобильный вариант.

## UI-022 — Сделать таблицы Backlog и Experiments рабочим инструментом сканирования

- **Status:** DONE
- **Priority:** Medium
- **Area:** UI / UX
- **Type:** Enhancement
- **Summary:** Усилить статус и связь «гипотеза ↔ эксперимент» в desktop-first таблицах, не расширяя их до перегруженного представления.
- **Description:**
  Таблицы `/backlog` и `/experiments` рассчитаны на работу с базой до сотен записей. Сохраняем текущую сдержанную визуальную систему, фильтры, сортировки, bulk actions, существующие карточки и доменную логику. В Backlog главным сигналом строки остаётся статус гипотезы, вторым — наличие и актуальное состояние связанных экспериментов. В Experiments главным сигналом остаётся текущая стадия и период, а связь с родительской гипотезой должна быть постоянно доступна. Для длинных имён и комментариев нужен доступный способ получить полный контекст; таблица не должна превращаться в тесное внутреннее окно прокрутки. Мобильный вариант и изменения модели данных не входят в эту задачу.
- **Acceptance Criteria:**
  - Backlog позволяет по каждой строке понять статус гипотезы и наличие, количество либо текущую стадию связанных экспериментов, с переходом к релевантному контексту.
  - Experiments постоянно показывает доступную связь с родительской гипотезой, не заменяя основное открытие карточки эксперимента.
  - Визуальная иерархия строки делает статус главным, связь с экспериментом/гипотезой — вторым, а Score и Funnel Level — поддерживающими сигналами.
  - Длинные названия, комментарии и связанные значения можно прочитать полностью доступным способом без потери текущего контекста.
  - Список остаётся удобным для сканирования нескольких строк: header и колонки не теряют читаемость из-за чрезмерно ограниченной рабочей высоты.
  - Существующие URL-фильтры, сортировки, inline-редактирование статуса/стадии, bulk actions и правила жизненного цикла «гипотеза ↔ эксперимент» работают без изменения.
  - Не меняются карточки `/backlog/[id]` и `/experiments/[id]`, Prisma-схема, доменная логика и мобильный вариант.
